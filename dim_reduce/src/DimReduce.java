import rnd.JohnsonLindenstrauss;
import util.RndProjMatrix;

import java.util.ArrayList;

/**
 * Created by miul on 4/19/2015.
 */

/**
 *              NOT USED
 *
 * Only exists to provide the possibility to run this module on its own.
 * All control is moved to ctrl_panel for centralization
 *
 */

public class DimReduce {
    private ArrayList points;
    private double precision;
    private double minDim;

    public void DimReduce(ArrayList data, double precision){
//        this.points = data;
//        this.precision = precision;
//        // for the number of oints, calculate minDim
//        JohnsonLindenstrauss jl = new JohnsonLindenstrauss(this.points.size(), this.precision);
//        this.minDim = jl.mindim();
//
//        // create a random projection matrix
//        RndProjMatrix rpm = new RndProjMatrix();
    }

}
